FROM rust:1.95-bookworm AS builder

WORKDIR /app

COPY . .
RUN cargo build --release -p hive_api --bin hive-api

FROM debian:bookworm-slim AS runtime

WORKDIR /app

COPY --from=builder /app/target/release/hive-api /usr/local/bin/hive-api

ENV BIND_ADDR=0.0.0.0:3000
EXPOSE 3000

CMD ["hive-api"]
